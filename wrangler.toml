name = "ocrcsv"
main = "src/worker.js"
compatibility_date = "2024-09-28"

[site]
bucket = "./dist"

# R2 bucket for image storage (optional)
# [[r2_buckets]]
# binding = "OCRCSV_IMAGES"
# bucket_name = "ocrcsv-images"

# Note: Custom domain (conocrcsv.mylastwork.net) should be configured
# in Cloudflare Dashboard: Workers & Pages → ocrcsv → Settings → Triggers → Custom Domains